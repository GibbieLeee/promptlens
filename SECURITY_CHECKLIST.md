# ✅ Чеклист безопасности перед деплоем

## Что было исправлено:

1. ✅ **Добавлен `.env` в `.gitignore`** - файлы с секретами теперь не попадут в репозиторий
2. ✅ **Firebase конфигурация вынесена в переменные окружения** - больше нет захардкоженных API ключей в коде
3. ✅ **Все секреты теперь хранятся только в `.env`** - который не попадет в Git

## ⚠️ ВАЖНО перед загрузкой на GitHub:

### 1. Создайте файл `.env` локально (если еще нет):
```env
VITE_GEMINI_API_KEY=ваш_gemini_ключ
VITE_FIREBASE_API_KEY=ваш_firebase_api_ключ
VITE_FIREBASE_AUTH_DOMAIN=ваш_проект.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=ваш_project_id
VITE_FIREBASE_STORAGE_BUCKET=ваш_проект.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=ваш_sender_id
VITE_FIREBASE_APP_ID=ваш_app_id
```

### 2. Проверьте, что `.env` НЕ в Git:
```bash
git status
# Файл .env НЕ должен появиться в списке изменений
```

### 3. Если `.env` уже был закоммичен ранее:
⚠️ **КРИТИЧНО:** Если вы ранее коммитили `.env` файл, его нужно удалить из истории Git:

```bash
# Удалить .env из Git (но оставить локально)
git rm --cached .env

# Если нужно удалить из истории (ОСТОРОЖНО!):
git filter-branch --force --index-filter "git rm --cached --ignore-unmatch .env" --prune-empty --tag-name-filter cat -- --all
```

**Лучше всего**: Если репозиторий еще не публичный, можно пересоздать его без истории.

## 🚀 Настройка Vercel:

### После загрузки проекта на GitHub:

1. Подключите репозиторий к Vercel
2. В настройках проекта (Settings → Environment Variables) добавьте **все** переменные из `.env`:
   - `VITE_GEMINI_API_KEY`
   - `VITE_FIREBASE_API_KEY`
   - `VITE_FIREBASE_AUTH_DOMAIN`
   - `VITE_FIREBASE_PROJECT_ID`
   - `VITE_FIREBASE_STORAGE_BUCKET`
   - `VITE_FIREBASE_MESSAGING_SENDER_ID`
   - `VITE_FIREBASE_APP_ID`

3. Убедитесь, что переменные добавлены для нужных окружений (Production, Preview, Development)

4. После добавления переменных - пересоберите проект

## 🔒 Проверка безопасности:

### Убедитесь что в коде нет:
- ❌ Захардкоженных API ключей
- ❌ Паролей
- ❌ Секретных токенов

### Все секреты должны быть:
- ✅ Только в `.env` (локально)
- ✅ В переменных окружения Vercel (на продакшене)
- ✅ В `.gitignore` (чтобы не попасть в Git)

## 📝 Что можно безопасно коммитить:

- ✅ `src/firebase.js` - теперь использует переменные окружения
- ✅ `src/api/gemini.js` - использует переменные окружения
- ✅ `.gitignore` - защищает `.env` файлы
- ✅ `README.md` - инструкции без реальных ключей
- ✅ Все остальные файлы проекта

## ⚠️ Примечание о Firebase API ключах:

Firebase API ключи на клиенте технически видны всем в браузере (это нормально для Firebase). Но использование переменных окружения:
- ✅ Позволяет легко менять конфигурацию для разных окружений
- ✅ Защищает от случайной публикации реальных ключей в коде
- ✅ Соответствует лучшим практикам разработки

---

**Теперь ваш проект готов к безопасной публикации! 🎉**

